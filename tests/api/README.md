# API test notes

This directory contains lightweight fixtures and notes for the draft management API.

Planned automated coverage:
- start with API disabled by default
- enable API on localhost with token auth
- validate config document
- replace config document
- request apply and assert reload intent is surfaced
