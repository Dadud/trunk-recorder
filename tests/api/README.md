# API test notes

This directory contains lightweight fixtures and notes for the draft management API.

Current coverage in this draft branch:
- API-enabled sample fixture shape validation
- required config fields present
- helper file references present
- supported source driver fixture validation

Still needed for a later follow-up:
- true end-to-end HTTP integration tests against a running binary
- runtime apply/reload verification against a live process
- helper file mutation tests against a temp config directory
